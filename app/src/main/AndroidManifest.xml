<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roberto.humansafari">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAHOh_Sg8o32duEkeWUK34g3epYjGEzhT8" />

        <activity android:name=".activity.player.JoinGameActivity" />
        <activity android:name=".activity.master.ListRankActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.player.ListGamePlayerActivity" />
        <activity android:name=".activity.master.ListGameMasterActivity" />
        <activity android:name=".activity.master.CreateGameActivity" />
        <activity android:name=".activity.master.MasterMainActivity"/>
        <activity android:name=".activity.player.PlayerMainActivity"/>
        <activity android:name=".activity.master.NewCharacterActivity" />
        <activity android:name=".activity.character.CharacterLogin" />
        <activity android:name=".activity.character.QrcodeActivity"/>

        <service android:name=".MessageReceiver">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".InstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>